# lastwar-client

A from-scratch Go reimplementation of the *Last War: Survival Game* (`com.fun.lastwar.gp`) mobile
client's network layer: GSL RSA+AES bootstrap crypto, SFS2X wire protocol, and SFSObject binary
codec — enough to log in and drive a real game session over TCP without the Unity client.

Built by reverse-engineering the decompiled APK; see [`docs/`](docs/) for the full protocol
dossier — a [Mintlify](https://mintlify.com) site covering the wire format, crypto, the
~3,178-command catalog, entity IDs, and a running log of what's confirmed live against real
production servers versus what's still static analysis. Start with
[`docs/live-validation.mdx`](docs/live-validation.mdx) for the current confirmed-vs-unconfirmed
picture. Preview the docs locally with `mint dev` from inside `docs/`.

## Status

- **Fully working, live-confirmed:** GSL crypto, SFS2X packet framing (including Zstandard
  decompression), SFSObject codec, brand-new-guest-account login, email-verification account
  binding, resource collection across 13 confirmed building types (Farmland, Iron Mine, Gold Mine,
  Smelter, Material Workshop, Training Base, Oil Well, Drone Parts Workshop, Component Factory, and
  the four Season 6 Spore Factory tiers — 7 more types are wired in but still unconfirmed, see the
  building-type table in `docs/live-validation.mdx`), and a growing set of account-level
  automations: the "Armed Truck" idle reward, greeting city visitors, bulk-helping alliance
  members, claiming all mail and alliance gifts, donating to the alliance's currently-recommended
  tech, and both once-a-day VIP claims.
- **Open question, not currently confirmed:** reconnecting into an *established* real account's
  live game state. This was proven working end-to-end at the time it was captured — but that
  capture used the `ta` analytics blob's real device/anti-fraud sub-fields, which a later security
  fix (round 13) replaced with empty-string placeholders to close a credential-leak vector. Whether
  reconnect still succeeds against the code as it ships today hasn't been re-tested against
  production; see `docs/live-validation.mdx` for the full methodology and the current state of this
  open question.
- The earlier "reconnect blocked" / "init push never arrives" problems were never protocol-level
  gates — they were a token-identity mismatch (the client claimed Android while replaying an
  iOS-issued token) and an unimplemented Zstd decoder, respectively. A real server merge later
  exposed a third class of the same kind of bug: `serverInfo` zone-migration redirects were either
  unhandled or unreachable on both login paths. See `docs/live-validation.mdx` for the full
  root-cause writeups.
- **Not yet general-purpose:** the reconnect path currently needs a session config captured from a
  real client login (see below) rather than deriving one from scratch. A from-scratch login using
  `-cs-ios`-equivalent identity from the very first GSL call hasn't been tried yet.

## Scope

Interoperability research: understanding the client-server contract well enough to build a
compatible, from-scratch client. Not in scope: gameplay strategy, economy optimization, or
anything that reads as a cheating/exploit guide rather than protocol documentation — see
[`docs/AGENTS.md`](docs/AGENTS.md) for the full boundary.

## Build

```
go build -o lastwar-client .
go test ./...
```

## Session config (recommended — avoids passing every `-cs-*` flag each run)

Reconnecting into an established account needs several values that can only come from a real
client's own login (device ID, access token, ShuMei fingerprint, ...). Rather than typing them on
the command line every time, put them in a JSON file:

```
cp config.example.json ~/.lastwar_goclient_session.json
chmod 600 ~/.lastwar_goclient_session.json
# then edit it with your own real values
```

`~/.lastwar_goclient_session.json` is auto-loaded on every run if present — no flag needed. To use
a different file, pass `-config /path/to/file.json`. Individual `-cs-*` flags still override
whatever the config file says, for one-off tests.

```json
{
  "ip": "203.0.113.10",
  "port": 17783,
  "zone": "your-real-zone-e.g.-APS1234",
  "gameUid": "your-real-composite-gameUid",
  "deviceId": "your-real-device-id_n3d",
  "shumeiBoxId": "your-real-shumei-fingerprint-token",
  "accessToken": "your-real-access-token-from-a-captured-login",
  "iosMode": true
}
```

**Where these values come from:** capture a real login (e.g. `tcpdump` while the real app logs in,
since the SFS2X game socket is plain TCP with no TLS) and decode the `Login` request — see
`docs/capturing-and-decoding-traffic.mdx` for the exact methodology. `gameUid`/`ip`/`port`/`zone`
also show up in a GSL `getserverlist` response's `serverList[]` entries. The access token is not
single-use, but it *is* bound to the platform identity (`iosMode`) it was issued under, and it will
eventually need refreshing from a fresh capture.

**Recognizing an expired token, confirmed live:** every command starts failing with
`CROSS-SERVER LOGIN FAILED: ec=28 full={ep=[E011], ec=28}` — the connection succeeds, but login
itself is rejected, so nothing downstream even gets attempted. Don't confuse this with a single
flaky run: it was 100% reproducible across 16 consecutive scheduled runs (every 3 hours for ~42
hours) until the credentials were refreshed. Fixing it needs a fresh capture, same as initial setup
— and in the one real recurrence so far, `shumeiBoxId` had also changed, not just `accessToken`, so
re-extract both from the new capture rather than assuming only the token moved.

**This file contains live credentials for a real account — keep it out of version control** (it's
already outside the repo, in your home directory, and `chmod 600`'d above; don't move it into
this repo or commit it anywhere).

## Usage

```
# One-time setup: see "Session config" above, then everything below just works with no flags.

# Collect resources from every confirmed building type, plus the Armed Truck idle reward,
# greeting city visitors, helping alliance members, claiming all mail and alliance gifts,
# donating to the recommended alliance tech, and both once-a-day VIP claims:
./lastwar-client -collect

# Just list buildings without collecting:
./lastwar-client -list-buildings

# Stay connected and issue ad-hoc test commands without re-authenticating. NOTE: since
# -collect isn't passed here, the full building list still prints to stdout once at
# startup by default (this is true of every run that omits -collect, not just -interactive
# ones -- pass -collect to suppress it, or -list-buildings to make the print explicit).
# Only flat scalar params (strings/bools/numbers) are supported over this control FIFO --
# nested/array-shaped params (e.g. the "heroes" array docs/military-battle.mdx documents for
# StartArenaBattleMessage) can't be exercised this way today:
mkfifo /tmp/lw_cmd_pipe
./lastwar-client -interactive /tmp/lw_cmd_pipe &
echo 'building.production.collect {"uuid":123}' > /tmp/lw_cmd_pipe

# Override specific config fields for a one-off test (e.g. a different captured token):
./lastwar-client -collect -cs-at <a-different-access-token>

# Brand-new guest account instead (always works, no email or config needed):
./lastwar-client -list-buildings -no-config

# Bind a guest session to a real account via email verification (only needed once,
# to obtain a fresh config -- see docs/capturing-and-decoding-traffic.mdx for turning
# that into a session config):
mkfifo /tmp/lw_code_pipe
./lastwar-client -email you@example.com -code-pipe /tmp/lw_code_pipe &
echo 123456 > /tmp/lw_code_pipe
```

Device identity also persists across runs in `~/.lastwar_goclient_*` (deviceId, username, gameUid,
loginKey) independent of the session config, so repeated guest/email-flow runs present a consistent
device to the server. Delete those files to start fully fresh.

## Running unattended (cron)

Confirmed live: `-collect` on a schedule, on a separate machine from wherever the session config
was captured. The binary is a single static executable, so this is just cross-compiling and
copying two files:

```bash
# Build for the target machine (adjust GOOS/GOARCH -- this example is Linux x86_64):
GOOS=linux GOARCH=amd64 go build -o lastwar-client-linux-amd64 .

# Copy the binary and the session config (the binary is useless without it):
scp lastwar-client-linux-amd64 user@host:~/lastwar-client/lastwar-client
scp ~/.lastwar_goclient_session.json user@host:~/.lastwar_goclient_session.json
ssh user@host 'chmod +x ~/lastwar-client/lastwar-client; chmod 600 ~/.lastwar_goclient_session.json'

# Cron entry -- set HOME explicitly, cron's default environment can't be assumed to have it:
ssh user@host "cat <<'CRONEOF' | crontab -
HOME=/home/user
0 */3 * * * /home/user/lastwar-client/lastwar-client -collect >> /home/user/lastwar-client/logs/collect.log 2>&1
CRONEOF"
```

Two things worth checking after setup, not just once but as ongoing habits:

- **Actually confirm cron itself fires the job**, not just that the binary runs when you invoke it
  manually over SSH — those aren't the same test. `crontab -l` accepting the entry doesn't prove
  the daemon is running or the schedule is right. Add a one-off entry a couple of minutes out,
  wait for it, and check both the log file *and* cron's own record of running it
  (`grep CRON /var/log/syslog` on Debian/Ubuntu) before trusting the real schedule.
- **The log file has no rotation** in the example above — it just appends forever. At 8 runs/day
  it takes a long time to matter, but for a long-lived deployment either cap it with `logrotate`
  or watch its size periodically.
- **Check the log's error rate periodically, not just whether the process is still running.** A
  cron job can "work" (exit 0, get invoked on schedule) while every run inside it is failing at
  the login step — see the token-expiry note in "Session config" above. Grepping the log for
  `"level":"ERROR"` and checking *when* errors started (not just whether any exist) is what
  actually catches this — see `docs/live-validation.mdx`'s serverInfo-redirect section for a
  real example of a failure that looked identical on every single run once it started.
- **Exit code 2 means the session itself is stale, not a transient blip.** Login/auth failures
  (both the plain-login and cross-server-reconnect paths) exit `2` specifically, distinct from
  the generic exit `1` used for other failures -- a cron wrapper can check `$?` directly and
  know to recapture a fresh session (see "Session config" above) without needing to grep the log
  at all.
- **`-log-level` controls the JSON log verbosity** (`debug`, `warn`, or `error`; default `info`) --
  handy for trimming a noisy cron log down to warnings/errors only, or turning on `debug` output
  while chasing down a problem run.

## Files

| File | Purpose |
|---|---|
| `crypto.go` | GSL RSA-PKCS1v15 + AES-256-ECB-PKCS7 request/response envelope; `pkcs7Unpad` rejects a claimed pad length larger than the block size instead of only bounding it by the ciphertext length; its byte-by-byte padding-match loop and `aesECBDecryptPKCS7`'s ciphertext-length guard (both already correct) now have regression coverage; `GSLCrypto.EncryptRequest` now only commits its new salt after both the RSA and AES encryption steps succeed, instead of eagerly right after generating it -- a failed call no longer leaves a stale, never-actually-sent salt that would silently defeat `DecryptResponse`'s empty-salt guard on reuse |
| `sfsobject.go` | SFSObject/SFSArray binary codec (encode + decode), recursive pretty-printing. Both `SFSObject` and `SFSArray` are safe by default: `String`/`GoString` delegate to `StringRedacted`, so Go's automatic `fmt.Stringer`/`fmt.GoStringer` invocation (`%v`/`%s`/`%#v`, `Println`, slog's Any-kind formatting) can never leak a raw dump; a bare `*SFSArray` reached with no enclosing key to check (no context to lean on) blanket-masks every item, while an array reached as a value already sitting under a known-non-sensitive key stays fully readable for debugging. `StringRedacted` is fail-closed: it explicitly masks known-sensitive fields (`loginKey`/`at`/`rt`/`accessToken`/`airKey`/`shumeiBoxId`/`pw`/`password`/`verifyCode`/`deviceId`/`chatToken`/`tk`/`ta`/`mail`/`gcmRegisterId`/`parseRegisterId`/`googleName`/`mt`/`simOp`/`simOpName`/`phone_model`/`osVersion`/`phone_screen`/`phone_native_screen`, plus the device/ad-identifier PII cluster `IMEI`/`AndroidID`/`androidDid`/`idfa`/`idfv`/`gaid`/`afuid`/`firebaseId`/`distinct_id`), matched case-insensitively via `isSensitiveSFSKey` so a mis-cased operator-typed key (e.g. `-interactive`'s `LoginKey` vs. the registered `loginKey`) still gets redacted, including inside a sensitive key's own primitive-array, `SFSArray`-wrapped, or nested-`SFSObject` value, gracefully on a nil nested value instead of panicking -- and any value shape under a sensitive key it doesn't explicitly recognize as safe falls through to a fixed `[REDACTED]` placeholder rather than to a formatter that might print real content. `EncodeObject`/`writeValuePayload` return a clean error instead of panicking on a nil nested `*SFSObject`/`*SFSArray` -- including `EncodeObject` itself on a nil top-level `*SFSObject`. `maxDecodedNodes` charges each primitive array's actual element count (not a flat 1 per field) against the decode-size budget, except `sfsByteArray`, whose 1:1 wire-to-memory ratio needs no separate charge. Every decoded field key AND string value passes through `sanitizeForTerminal` before reaching `StringRedacted`'s output, escaping C0 control bytes/DEL as `\xHH` -- closing a terminal-injection gap where a malicious server or crafted capture file could otherwise embed raw ANSI/OSC escape sequences that spoof terminal output at the two bare `fmt.Printf` sinks (`decode.go`, `buildings.go`) that print it directly. Bare `SFSValue` (what `Get()` returns) now also has a redacting `String`/`GoString` (blanket-masked, since it carries no key context to check against `sensitiveSFSKeys`), mirroring `*SFSObject`/`*SFSArray`'s own treatment; `StringRedacted`/`formatSFSValueRedacted` now enforce a per-call `maxFormattedNodes` format-time budget (independent of the decode-time `maxDecodedNodes`), truncating with a visible marker instead of letting one call recurse unboundedly over an already-decoded object; `sensitiveSFSKeys` now also covers `un` (the classic SFS2X username field) and `googlePlay`; `Has`/`Get` now each have their own doc comment instead of sharing one misattached to `Has` alone |
| `packet.go` | SFS2X packet framing: header flags, length-derived XOR, zlib compression, Zstandard decompression; every field read after the leading header byte converts a bare `io.EOF` into `io.ErrUnexpectedEOF`, so a capture truncated mid-frame is never misreported as a clean end-of-stream; a closed/exhausted reader (`io.EOF`/`io.ErrUnexpectedEOF`, e.g. a peer's graceful TCP close) is now also wrapped (`deadConnError`) to additionally satisfy `net.Error` with `Timeout()==false` -- previously neither sentinel satisfied `net.Error` at all, so a real peer close silently defeated every "abort remaining work on a genuine dead connection" check built across rounds 16-23 in buildings.go/mail.go/visitors.go/alliance.go/interactive.go, none of which ever actually fired for this, the single most realistic close scenario |
| `gsl.go` | HTTP bootstrap: check-version, GSL `getserverlist.php` (`opt=new\|login\|fix\|refresh`); `LoginServerListRespon.Code` is `flexString` (tolerates both JSON-string and bare-number shapes, matching its sibling `CheckVersionResponse.Code`); `encodeFormSorted` errors instead of silently dropping a form field absent from its `order` whitelist; `applyLoginServerFallback` synthesizes a `ServerList` entry from `LoginServer` when `opt=new` returns an empty `ServerList`, a conservative fallback for a scenario never yet observed live; `GetServerList` now fails clearly when the response's `bin` field is present but empty, instead of silently falling through to unmarshal the original envelope into a zero-valued, nil-error result (previously a fully silent no-op on the serverInfo-redirect refresh path); `checkVersionHosts`' doc comment no longer names a nonexistent identifier |
| `conn.go` | TCP connection, envelope send/receive, heartbeat; `classifyResponse`'s `benignErrorCodes` are now scoped by `cmd`, not just errorCode value (each entry maps to the specific cmd(s) it's actually documented for), so a genuine failure on an unrelated command sharing the same numeric errorCode no longer gets silently reclassified as benign; `DoHandshake`'s own wall-clock-deadline read loop now returns `login.go`'s `deadlineExceededError` (a genuine `net.Error` with `Timeout()==true`) when the deadline elapses between reads, instead of a bare `fmt.Errorf` -- previously the one other independent read loop (besides `waitFor`) hitting the exact bug class round 23 fixed; `sendAndWait`'s send-stage failure (a failed `SendExtension`/write) is now wrapped in `sendStageError`, a `net.Error` forcing `Timeout()==false`, so it's no longer indistinguishable from `waitForCmd`'s benign wait-stage timeout outcome when the underlying write failure itself happens to report `Timeout()==true` (e.g. a deadline-exceeded write) -- previously every collect/claim orchestration loop's non-timeout-abort logic could misclassify a genuinely dead connection as "just a slow response, keep going"; `Close()` is now genuinely idempotent -- the underlying `net.Conn.Close()` call moved inside the same `sync.Once` as the `stopHeartbeat` channel close, with its result captured and returned on every call, instead of running unconditionally outside the `Once` and risking a spurious second error on a realistic concurrent double-`Close()` |
| `errors.go` | `ErrAuthRejected` sentinel distinguishing exit code 2 (confirmed auth rejection) from exit code 1 (generic failure) |
| `identity.go` | Persisted device identity + the ~50-field SFS `Login` params object (Android and iOS variants); the iOS-only `ta` analytics blob is diagnostic-only placeholders, not the real deviceId/airKey/shumeiBoxId values, since that blob is a single opaque JSON string `StringRedacted` can only mask wholesale; `loadOrCreateDeviceIdentity` no longer treats a non-ENOENT read failure on an existing state file as "no identity yet" (which used to silently fabricate and persist a replacement device ID), and creates a fresh device-ID file with `O_EXCL` instead of an unconditional overwrite; a pre-existing-but-empty state file (e.g. left behind by a process that crashed between `O_CREATE\|O_EXCL` and writing its content) self-heals with a fresh identity via an atomic write-temp-then-rename after a short bounded retry window gives any genuine concurrent writer a chance to finish first, instead of permanently failing every subsequent run; `saveStateFile` (backing `SaveLoginKey`/`SaveGameUid`/`SaveUsername`) now goes through that same atomic write-temp-then-rename helper instead of a plain `os.WriteFile`, closing the same torn-write-on-crash gap for those files too; `createDeviceIDStateFile` (the primary first-run device-id creation path) now also writes through a fsync'd temp file, publishing via `os.Link` rather than a direct `O_CREATE\|O_EXCL` write -- closing the same torn-write gap there too while preserving `os.Link`'s exclusivity (fails on an existing destination, unlike `os.Rename`) so the existing race-detection semantics are unchanged; `BuildLoginParams`' `psh` field is deliberately NOT switched under `IOSMode` (documented, not an oversight -- no live iOS capture exists to isolate what a genuine iOS client sends for it, unlike every other IOSMode-gated field); `BuildLoginParams`' doc comment (previously misattached to `LoginParamsInput`, three declarations earlier) now correctly attaches to the function itself |
| `config.go` | Session config file loading (`~/.lastwar_goclient_session.json` / `-config`); `loadEffectiveConfig` fails loudly (`os.Exit(1)`) on any non-`ENOENT` read error on either an explicit `-config` path or the default path, instead of silently treating a permission/I-O glitch on an existing file the same as "no config yet" and diverting into an unrelated guest/email login flow; `SaveSessionConfig` now writes via the same atomic write-temp-then-rename helper identity.go's persisted state files use, instead of a plain `os.WriteFile`, closing the same torn-write-on-crash gap for a file that holds a live access token |
| `login.go` | Full login orchestration: guest login, email verification, account binding; the operator's email address is logged only as a length (`emailLen`), never in cleartext; `waitForInitPush` returns its terminal read error so a real connection failure while waiting for `init` is logged distinctly from an ordinary timeout, and `Login` now closes the connection and returns an error immediately on that case instead of falling through to a nominally-successful result wrapping a dead connection; `buildBaseZoneLoginAddr` fails clearly instead of silently dialing the loopback interface when the resolved ip is empty, mirroring `main.go`'s cross-server guard, and is now also used by `Login`'s own mid-login `serverInfo` redirect branch (previously unguarded there) as well as the initial dial, and also rejects a non-positive port (previously a missing/unparseable redirect port silently built a `"host:0"` address); `waitForInitPush`'s active-pull fallback now treats a failed `SendExtension("login.init", ...)` the same as a failed read -- a definite connection failure returned immediately, not merely logged and fallen through into another blocking wait; `redact` now slices by rune, not byte, boundary, so a multi-byte UTF-8 sensitive value (e.g. a CJK `googleName`, an internationalized `mail` address) redacts to valid UTF-8 instead of a truncated/invalid byte sequence; `Login`'s email-verification path now warns (instead of silently discarding the error) when persisting the received `gameUid`/`gameUserName` fails, matching every other `SaveGameUid`/`SaveUsername`/`SaveLoginKey` call site in this file; `waitFor`'s wall-clock-deadline-elapsed exit now returns a `net.Error` with `Timeout()==true` (`deadlineExceededError`) instead of a bare `fmt.Errorf` -- previously indistinguishable from a genuine dead connection to every downstream `errors.As`-against-`net.Error` check (buildings.go/mail.go/visitors.go/alliance.go/interactive.go), most visibly killing an entire `-interactive` session over an ordinary near-deadline non-matching push; `redact` now scales how much of the input it reveals with length instead of a flat first-4/last-4, so a realistic 9-20 character password (also redacted via this function, alongside long opaque tokens) no longer exposes a majority of itself; `waitForInitPush`'s `init`-push return now dedupes buildings/visitors by uuid (`dedupeBuildings`/`dedupeVisitors`), matching the protection `buildings.go`'s fallback `FetchBuildings` path has had since round 12 -- previously the PRIMARY init-push path had no dedup at all, so a repeated uuid in one push would cause a real, redundant network request; `Login` no longer logs the operator's persisted or server-returned account username in cleartext at Info level (`usernameLen` now, mirroring the existing `emailLen` pattern) -- previously an unconditional cleartext leak on every run/login |
| `crossserver.go` | Direct role reconnect (`CrossServerLogin` reimplementation); `LWDEBUG_DUMP_LOGIN`/`LWDEBUG_DUMP_LOGIN_BODY` env vars dump the outgoing login request for debugging (the latter now writes via `identity.go`'s atomic write-temp-then-rename helper instead of a plain `os.WriteFile`+`os.Chmod`, closing the same torn-write/temporary-loose-permissions gap already closed elsewhere for credential-bearing files); `DoCrossServerLogin`'s own mid-login `serverInfo` redirect branch AND its initial dial now both route through `login.go`'s `buildBaseZoneLoginAddr` (instead of an unguarded `firstHost(...)`-based address), failing clearly instead of silently dialing loopback on a malformed ip/port |
| `buildings.go` | Building list parsing, type-ID table, resource-collection commands; `FetchBuildings` dedupes visitors across repeated `init` pushes the same way it already dedupes buildings; `CollectAll` aborts its remaining sub-actions early on a genuine, non-timeout connection-level (`net.Error`) failure instead of still waiting out every remaining action's timeout in turn -- an ordinary per-action response timeout (`net.Error` with `Timeout()==true`, sendAndWait's routine outcome on a perfectly healthy connection) no longer misfires this abort and skips every remaining action, including all building collection; `FetchBuildings`' per-push 3-second deadline extension is now capped against the caller's original timeout budget (`capDeadline`), instead of unconditionally resetting on every qualifying `init`/`push.init.build` push -- closing a liveness gap where a peer that kept re-sending either push type faster than the 3s window could keep the call, and the whole synchronous `main()` flow, hanging indefinitely; `CollectAll`'s outer abort check now walks a sub-action's full (possibly `errors.Join`-ed) error tree via `containsNonTimeoutNetError` instead of a plain `errors.As` (which only finds the first net.Error in the tree) -- a genuine non-timeout `net.Error` buried behind an earlier benign timeout inside one sub-action's own internal loop no longer gets masked into a false "keep going"; `FetchBuildings`' `push.init.build` deadline-shrink is now gated on the authoritative `init` push having already arrived, instead of firing unconditionally -- a `push.init.build` arriving before a delayed `init` no longer risks cutting the wait short before the authoritative `building_new` data ever gets read; `CollectAll`'s per-building collect loop is now capped at `maxCollectibleBuildingsPerRun` (300, a defensive non-protocol sanity ceiling, not a guessed real limit) -- previously unbounded, so a peer reporting an implausibly large building count could hang the entire run for hours instead of the ~40-minute worst case the cap now bounds it to; `ParseInitBuildings` and `FetchBuildings`' two sibling inline loops (`push.init.build`'s `defaultBuilds`, `push.add.building`'s `buildings`) now cap the number of RAW items examined per push at `maxRawBuildingItemsPerPush` (2000), not just valid ones appended to the output -- previously unbounded on every dimension (worse than `visitors.go`'s pre-existing cap), and `ParseInitBuildings` in particular feeds the PRIMARY init-push path (`login.go`'s `waitForInitPush`), so a peer padding `building_new` with malformed entries could force a full scan-and-log cost on every login; a new type-aware `requireFieldType` guard (used by `ParseInitBuildings` and both inline loops' `uuid` checks) now rejects a present-but-wrong-typed field instead of letting it silently coerce to zero via `GetLong`'s own fallback and collide with a genuinely-zero or another wrong-typed uuid in every uuid-keyed dedup map; `PrintBuildings` now sorts its `byType` map's keys before iterating, so stdout's block order is deterministic across runs instead of following Go's randomized map iteration order |
| `visitors.go` | City-visitor parsing (from the `init` push) and greeting (`visitor.operate`); `GreetVisitors` aborts its remaining (`maxNum`-bounded) visitors immediately on a genuine, non-timeout `net.Error`, instead of burning a full command timeout per remaining visitor against an already-dead connection -- an ordinary per-visitor response timeout no longer misfires this abort; `ParseInitVisitors` now actually enforces the `init` push's own `maxNum` field as a hard cap on `visitor.list` (falling back to a defensive `maxVisitorsDefensiveCeiling` of 25 if absent/nonsensical), instead of the cap being only a doc-comment claim with no code behind it -- previously unbounded against a peer sending an implausibly long list; the server-supplied `maxNum` itself is now also clamped against a hardcoded `maxVisitorsUpperBound` (300), closing a bypass where a hostile peer could set `maxNum` to an enormous value and completely defeat the defensive ceiling by trusting its own attacker-controlled input as the enforcement mechanism; `ParseInitVisitors`' cap now also bounds the number of RAW `visitor.list` items examined, not just valid ones appended to the output -- previously a malformed entry's `continue` didn't count against the cap, so a peer padding the list with minimal malformed entries (bounded only by the much larger decode-level node budget) could still force the full scan-and-log cost regardless of the configured cap; `uid` is now guarded via the type-aware `requireFieldType` (buildings.go) instead of presence-only `requirePresentField`, so a present-but-wrong-typed uid no longer silently coerces to 0 and collides with a genuinely-zero or another wrong-typed uid in `FetchBuildings`'/`login.go`'s dedup maps |
| `mail.go` | Mailbox listing/pagination and batch reward claiming, scoped per category; `groupUnclaimedByType` skips (with a warning) any reward-bearing mail whose `type` field is missing/explicit-null instead of defaulting it into a `type=0` batch; `ListMail` warns when it exhausts its 20-page pagination cap while the server still reports more mail available, instead of silently truncating; `ClaimAllMail` now processes whatever partial mail `ListMail` already collected before a mid-pagination failure, instead of discarding it; both `ClaimAllMail` batch loops (read-status and per-type reward-claim) now abort their remaining batches immediately on a genuine, non-timeout `net.Error` instead of burning a full command timeout per remaining batch against an already-dead connection -- an ordinary per-batch response timeout no longer misfires either abort; `ClaimAllMail` also checks `ListMail`'s own returned error for a non-timeout `net.Error` before entering the read-status loop at all, skipping straight to returning instead of issuing one more batch against an already-known-dead connection; `ListMail`'s pagination loop now dedupes by uid across pages, so a repeated uid (should the server's pagination cursor ever resend one) can't flow into `ClaimAllMail` twice; `ListMail` now warns if the response's `more` field is present but not a bool, instead of silently treating it as `more=false` and stopping pagination with zero diagnostic; `ListMail` also now warns if `lastMailTime` is missing/null on a `more=true` page (its sibling `lastUid` cursor field was already guarded, but `lastMailTime` wasn't, and both silently collapse to a value indistinguishable from a legitimate one via `GetLong`'s zero-value behavior); `ListMail`'s per-page mail array loop now caps the number of RAW items examined at `mailListRawItemCap` (1000, now package-scoped for consistency with its siblings), independent of `mailListPageSize`/`maxPages` (which bound round-trip count only, not a single page's array size) -- previously a malformed entry's `continue` didn't advance any output-based cap; `ListMail`'s `uid` check and `groupUnclaimedByType`'s `type` check now use the type-aware `requireFieldType` (buildings.go) instead of presence-only `requirePresentField`, so a present-but-wrong-typed uid/type no longer silently coerces to a zero value that permanently loses a reward-bearing mail's dedup slot or misroutes it into the wrong `type=0` claim batch |
| `alliance.go` | Alliance automation: help-all, gift claiming, recommended-tech donation; `ClaimAllianceGifts` aborts its remaining gift type immediately on a genuine, non-timeout `net.Error`, instead of also attempting it against an already-dead connection -- an ordinary per-type response timeout no longer misfires this abort; `DonateRecommendedAllianceTech` now warns if `allianceScience` is present but not the expected array shape, instead of silently no-opping; `findRecommendedTech`'s `allianceScience` scan now caps the number of RAW items examined at `allianceScienceRawItemCap` (1000), since it returns on the first `state==1` match and has no unbounded output growth of its own, only unbounded scan/log cost from malformed entries; `findRecommendedTech`'s `scienceId` check now uses the type-aware `requireFieldType` (buildings.go) instead of presence-only `requirePresentField`, so a present-but-wrong-typed `scienceId` no longer silently coerces to 0 and triggers a real donate attempt against the wrong tech; `ClaimAllianceGifts`' doc comment (previously misattached to the unrelated `allianceGiftPremium`/`allianceGiftRegular` const block) now correctly attaches to the function itself |
| `vip.go` | Once-a-day VIP claims: login-streak score and the VIP-level daily freebie chest -- see `vip_test.go` |
| `interactive.go` | Control-FIFO REPL for testing commands against a live session without re-login; a malformed JSON command logs only `cmd` and the parse error, never the operator's raw unparsed params text; trailing data after a well-formed JSON value now aborts the send with an error instead of being silently discarded; a line missing the required space between the command name and its JSON params (e.g. a JSON blob glued directly onto the command name) is now caught with a clear "bad JSON params" error instead of being silently sent as a mangled literal command with empty params; `handleInteractiveLine`'s response-wait failure now also applies the net.Error/`Timeout()` distinction (an ordinary "no matching response within 8s" stays non-fatal; a genuine non-timeout failure now exits the process, matching the adjacent send-failure handling, instead of silently returning to the FIFO prompt as if nothing had gone wrong); the example usage log line now shows `building.production.collect` instead of the stale `building.camp.collect` (which only covers 3 of the 13 confirmed building types); `handleInteractiveLine`'s response-wait now references the shared `defaultCmdTimeout` constant instead of a separately hardcoded `8*time.Second` literal |
| `decode.go` | `-decode-stream`: decode a reassembled capture file with the live codec, no network; a `DecodeObject` failure now prints only the error and the raw body's byte length, never a hex dump of the (pre-decode, unredacted) body content -- a truncated-but-otherwise-well-formed frame could have an intact sensitive field sorted near the front, which the old hex dump bypassed `sensitiveSFSKeys` entirely to expose |
| `main.go` | CLI entrypoint; warns when `-decode-label` is set without `-decode-stream` (a no-op combination, mirroring this file's other ignored-flag warnings); `runCrossServerTest` fails clearly (rather than silently dialing the loopback interface) when the resolved ip is empty, mirroring its existing port validation; `-interactive`'s help text documents its flat-scalar-only param limitation; a GSL `opt=refresh` response with no usable data now exits 2 (matching the README's own documented stale-session cron-exit-code contract), not the generic 1; `runCrossServerTest` warns at WARN (not silently at INFO) when a GSL refresh response's server list overrides an explicitly-passed `-cs-ip`/`-cs-port`/`-cs-zone`/`-cs-gameuid` flag, naming which flag(s) were overridden; the "ignoring -cs-at"/"continuing ... unrefreshed" warnings now only attribute the value to the `-cs-at` flag when it was actually typed on the command line, not when it came from a loaded session config (reusing the `-cs-ios`-style `fs.Visit` explicit-flag tracking); a stray non-flag (positional) argument left over after parsing -- most likely a mistyped flag missing its leading dash -- now aborts with a clear error instead of silently proceeding into a real run; an explicitly-passed `-config` path that doesn't exist now logs a `WARN` naming the path (`warnIfExplicitConfigPathNotFound`) instead of silently degrading into an unrelated guest-identity run with zero diagnostic; a `-cs-rt` refresh that leaves the run with literally no access token at all (not just an unrefreshed stale one) now also warns, distinctly from the "unrefreshed" case; `serverListOverrideFlags` now also requires the overridden `-cs-ip`/`-cs-port`/`-cs-zone`/`-cs-gameuid` value to actually be non-empty/non-zero (not just explicitly typed), fixing a false-positive "overriding" WARN for an explicitly-passed-but-empty flag; `-cs-gameuid`'s help text now notes it's also sent on every `-cs-rt` refresh call, not just `-cs-ip`; the unreachable `ErrAuthRejected`-gated exit-2 branch on a GSL refresh failure (never wrapped by `GetServerList`'s own error returns) was removed in favor of the one reachable `os.Exit(1)` fallback; `-email`'s help text now accurately describes the silent fresh-guest-identity fallback when omitted with no loginKey on file, instead of reading like a hard requirement; `-collect` combined with `-interactive` no longer silently discards the operator's explicit request to stay connected when `CollectAll` reports only ordinary benign-timeout/business-logic failures (was `os.Exit(1)`ing before the `-interactive` check was ever reached on ANY collect error) -- a new `shouldAbortBeforeInteractive` helper, applied at both this call site and `runCrossServerTest`'s identical one, still aborts unconditionally on a genuinely dead connection; `-cs-rt`'s help text now also documents that both `-cs-at` and the server list coming back empty is fatal (exit 2), not a graceful fallback like the single-field-empty case; a value-taking flag (e.g. `-email`) placed immediately before another flag with no intervening value -- which Go's flag package silently parses as swallowing that next flag's NAME as its own value -- is now detected (`detectSwallowedFlagValue`, scoped to flags where a leading `-` is never legitimate) and aborts with a clear error instead of silently proceeding with a corrupted value; `runCrossServerTest`'s port validation now distinguishes "explicitly passed but invalid" from "never given at all" instead of using the same misleading "no port given" wording for both; `shouldAbortBeforeInteractive` now also gates both `FetchBuildings` fallback call sites (previously only `CollectAll`'s two call sites), closing a sibling instance of the same bug -- a non-fatal `FetchBuildings` error (e.g. one malformed frame, not a genuine dead connection) no longer discards an explicit `-interactive` request; `crossServerSaveBackNeeded` now normalizes a pipe-delimited `origHost` via `firstHost` before comparing, instead of comparing the raw multi-host value against the single resolved host actually dialed -- previously a `-cs-ip` with a fallback host list would spuriously "save back" and permanently collapse to one host on the very first successful, non-redirected run; the zero-buildings fallback no longer unconditionally overwrites an already-obtained non-empty `visitors` slice from `Login()`'s own init-push parse; `runCrossServerTest` now also preflight-checks `gameUid` (mirroring its existing `ip`/`port` checks), since cross-server login sends `gameUid` directly as the wire `un` field (unlike base-zone login's expected-empty `un`) -- previously an empty `gameUid` burned a full dial+login round-trip only to fail with the same `ec=28/E011` signature README documents as an expired session, misdirecting debugging away from the real missing-flag cause; the `-cs-ip`/`-cs-gameuid` preflight checks now distinguish "explicitly passed but empty" from "never given at all" (mirroring the existing `-cs-port` wording split), instead of always claiming the flag was never given; the invalid-`-cs-port` error now logs `port` as a structured slog field instead of baking it into the message text; `-list-buildings`'/`-collect`'s help text now accurately describe `PrintBuildings`' actual output and both idle-reward tracks (Armed Truck/Overlord) |
| `selftest_test.go` | Unit tests for the crypto/codec/framing layers (no network required); `pkcs7Unpad` rejecting a claimed pad length above the AES block size on a corrupted multi-block plaintext, and separately rejecting a plausible-looking pad length whose earlier padding bytes don't actually match it; `aesECBDecryptPKCS7` rejecting an empty or non-block-aligned ciphertext |
| `crypto_gsl_test.go` | GSL crypto envelope round trip: `NewGSLCrypto` + `EncryptRequest` + `DecryptResponse` composed together, not just the AES-ECB/PKCS7 primitives `selftest_test.go` covers; a failed `EncryptRequest` call proven to leave `g.salt` unchanged from its pre-call value, not a stale never-sent salt |
| `gsl_http_test.go` | `CheckVersion` and `GetServerList` exercised against a fake HTTP server; `GetServerList`'s four response-error branches (HTTP status, top-level JSON, decrypted plaintext, plaintext fallback) proven to never leak the raw body/decrypted plaintext -- which can carry a live `at`/`rt` session token -- into the returned error; a string-typed `code` field decodes successfully via `LoginServerListRespon.Code`'s `flexString` type; `applyLoginServerFallback` synthesizing a `ServerList` entry from `LoginServer` for `opt=new` with an empty `ServerList`, and proven scoped to `opt=new` only (an `opt=fix` response with the same shape stays empty); a corrupted/tampered `bin` field (valid base64 but failing AES/PKCS7 decryption) proven to return a clear "decrypt GSL response" error without leaking the raw ciphertext; and a present-but-empty `bin` field proven to return a clear error instead of a nil-error zero-valued response |
| `gsl_form_sync_test.go` | Regression test proving `encodeFormSorted`'s `order` whitelist stays in sync with `GetServerList`'s actual `form.Set(...)` calls, by reading gsl.go's own source for both (the same source-scanning technique `main_flags_test.go`'s `TestCrossServerFlagNamesMatchesDeclarations` uses against main.go) -- catches the exact silently-dropped-field drift `encodeFormSorted`'s doc comment records a prior production bug from; `encodeFormSorted`'s own runtime backstop (erroring when a `form` field is absent from `order`) is exercised directly too |
| `packet_bigsized_test.go` | Packet framing round trip for payloads over 65535 bytes (4-byte length prefix, `hdrBigSized`) |
| `packet_zstd_test.go` | `ReadPacket`'s Zstandard-decompression branch (`hdrCompressed\|hdrUseLZ4`) round trip |
| `packet_oom_test.go` | `ReadPacket`'s declared-length size guards reject an oversized/hostile frame using only the header fields, before ever reading (let alone allocating) the body; a stream truncated exactly on a field-read boundary mid-frame (the shape a real cut-off capture produces) is proven to surface as `io.ErrUnexpectedEOF`, not be misclassified as a clean end-of-stream |
| `sfsobject_array_test.go` | Array-tag decode round trips (including `ByteArray`'s 4-byte element count vs. every other array tag's 2-byte count) and encode round trips; a hostile-input battery against the decoder -- negative array/text counts, the `maxNestDepth` recursion bomb, the `maxDecodedNodes` wide-fan-out amplification bomb (including the per-element-charged primitive-array variant), and `DecodeObject` rejecting trailing bytes left over after a well-formed top-level object |
| `sfsobject_encode_error_test.go` | `EncodeObject` returns an error instead of panicking when a string value exceeds the wire format's 65535-byte length-prefix limit, including through nested-`SFSObject` recursion |
| `sfsobject_redact_test.go` | `StringRedacted` masks every known-sensitive key (`loginKey`/`at`/`rt`/`accessToken`/`airKey`/`shumeiBoxId`/`pw`/`password`/`verifyCode`/`deviceId`/`chatToken`/`tk`/`ta`/`mail`/`gcmRegisterId`/`parseRegisterId`/`googleName`/`mt`/`simOp`/`simOpName`/`phone_model`/`osVersion`/`phone_screen`/`phone_native_screen`, plus the device/ad-identifier PII cluster, including inside nested `SFSObject`/`SFSArray` values, a sensitive key's own raw `SFSArray`-wrapped value, and all 8 primitive-array wire types) while leaving ordinary gameplay fields untouched, matches `String`'s output exactly when no sensitive keys are present, `BuildLoginParams(IOSMode: true)` proven to never embed live DeviceID/AirKey/ShumeiBoxId secrets in its `ta` analytics blob, `fmt.Sprintf("%v"/"%#v", ...)` on a raw `*SFSObject` proven to never leak via Go's automatic `fmt.Stringer`/`fmt.GoStringer` invocation, `%v`/`%s`/`%#v`/`Fprintln` on a bare `*SFSArray` proven to never leak a raw scalar item, a nil nested value proven to never panic, a legitimate large single `sfsByteArray` field proven to no longer spuriously fail against `maxDecodedNodes`, `redactSFSValue`'s fail-closed fallback proven to mask a scalar (`PutInt`/`PutLong`/`PutBool`/`PutDouble`), a decode-only `sfsFloat`/`sfsNull` value, or a nested `*SFSObject`'s own sub-fields under a sensitive key, `EncodeObject` returning a clean error (not panicking) on a nil nested `*SFSObject`/`*SFSArray` value or a nil top-level `*SFSObject`, a case-variant sensitive key (`LoginKey`/`LOGINKEY`/`lOgInKeY`, ...) still getting redacted via `isSensitiveSFSKey`'s case-insensitive lookup, ANSI/OSC terminal-escape sequences in a field value, redacted-value remainder, or key name itself never appearing unescaped in `StringRedacted`'s output (while plain newline/tab stay readable), a bare `SFSValue` extracted via `Get(...)` never leaking its real value through `%v`/`%s`/`%#v`/`Fprintln`/`String`/`GoString`; and `StringRedacted`'s `maxFormattedNodes` format-time budget proven to bound the cost/output size of a single call against an object with a very large internal array, truncating with a visible marker |
| `sfsobject_sensitive_keys_sync_test.go` | Self-enforcing completeness check tying `sensitiveSFSKeys` (sfsobject.go) to reality: statically scans every non-test `.go` file for a literal `Put*("key", ...)` call site (mirroring `gsl_form_sync_test.go`'s source-scanning technique) and fails loudly, naming the key and call site, if it lands in neither `sensitiveSFSKeys` nor this file's own reviewed `knownNonSensitiveSFSKeys` allowlist -- turning the "does sensitiveSFSKeys cover every field this repo actually sends?" question five straight audit rounds answered by manual re-audit into a CI check; also guards `knownNonSensitiveSFSKeys` against drift if a listed key is later promoted into `sensitiveSFSKeys` |
| `conn_test.go` | `Envelope.AsExtension`, `classifyResponse`'s success/benign/failure outcome classification, and a `GameConn` send/receive round trip; every legitimately-scoped benign errorCode+cmd pair (`602026`+`building.production.collect`, both `120289` cmds, `visitor_err_coming`+`visitor.operate`, `120471`+`al.science.donate`) still classifies as benign, a mismatched cmd sharing one of those errorCode values now falls through to a real failure instead of being silently swallowed; `logCommandResult`'s status=0/no-errorCode benign log line (previously unreached by any test despite the suite's own claim of covering "all three" classification branches); and `Close()`'s genuine idempotency, both sequential (3 calls invoke the underlying `net.Conn.Close()` exactly once and all return the same result) and concurrent (20 goroutines racing `Close()`) |
| `conn_wait_test.go` | `sendAndWait`/`waitFor`/`waitForCmd`/`waitForInitPush`: outcome classification, deadline timeouts, unmatched-push skipping, and the init-push halfway active-pull fallback; `waitFor`'s "skipped push while waiting" debug logger proven to never leak a raw `loginKey` when `push.account.login.new` is the message being skipped; `waitForInitPush` returning its terminal error distinctly from a genuine timeout; `buildBaseZoneLoginAddr`'s empty-ip guard, including against a `|`-delimited fallback list whose first entry is empty; the init-push halfway active-pull timing assertion widened to an eighth/seven-eighths band to absorb CI scheduler jitter; `Login()`'s own mid-login `serverInfo` redirect branch rejecting a malformed/empty redirect ip or a missing/zero port clearly instead of silently dialing loopback or building a `"host:0"` address; a failed `SendExtension("login.init", ...)` send now surfacing promptly as a genuine connection-failure result instead of silently falling through to keep waiting; `waitFor`'s wall-clock-deadline-elapsed exit (reached only after at least one non-matching envelope was already read) now returning a `net.Error` with `Timeout()==true`, deterministically isolated from the sibling per-read-timeout exit via a fake conn whose first `Read` sleeps well past the deadline; `deadlineExceededError`'s `Error()`/`Temporary()` methods directly (previously 0% covered); `ReadPacket`/`GameConn.ReadEnvelope` now returning a `net.Error` with `Timeout()==false` for a graceful peer close (both the between-packets and mid-frame cases), proven at both the packet-level and live-connection level; and `sendAndWait`'s send-stage (write) failure now being wrapped as a non-timeout `net.Error` (`sendStageError`) even when the underlying write failure itself reports `Timeout()==true`, distinct from `waitForCmd`'s genuinely benign wait-stage timeout |
| `conn_handshake_test.go` | `DoHandshake`'s success path and its `ec`-bearing failure path wrapping `ErrAuthRejected`; `StartHeartbeat`'s periodic pings, stop-on-`Close`, and close-on-send-failure behavior; the "skipped envelope while waiting for handshake" fallback log proven to never leak a raw credential; `DoHandshake`'s wall-clock-deadline-elapsed exit (reached only after a non-matching envelope was already skipped) now returning a genuine `net.Error` with `Timeout()==true`; and `DoHandshake`'s genuine `ReadEnvelope`-failure branch (previously untested, unlike its structural sibling `waitFor`) proven to still wrap the underlying error |
| `identity_test.go` | `BuildLoginParams`' Android/iOS and empty-vs-set-`GameUid` conditional field logic; `SaveLoginKey`/loose-permission warning and load/save round trip for the persisted device identity; `stateFilePath`'s `$HOME`-unavailable fallback proven to log a warning, not fail silently; `loadOrCreateDeviceIdentity` proven to never silently fabricate a replacement device ID on a non-ENOENT state-file read failure; a pre-existing-but-empty (no concurrency involved) device-id file proven to self-heal with a fresh identity instead of permanently failing every subsequent run; a genuine concurrent writer's content proven to still be adopted, not overwritten by the self-heal path; `saveStateFile`'s normal write/read-back round trip still working correctly now that it goes through the atomic write-temp-then-rename helper; `createDeviceIDStateFile`'s switch to write-temp-then-`os.Link` still producing the same end result for normal creation (no stray temp file left behind), still failing with an `os.IsExist`-shaped error against a pre-existing file (leaving its content untouched, unlike `os.Rename`), and never touching the final path before the content is fully durable; `loadOrCreateDeviceIdentity`'s username/loginKey read-failure propagation (previously only the `gameUid` sibling of this 3x-copied pattern was tested); and the genuine-concurrent-writer test's timing margin widened (writer delay reduced from 10ms to 3ms, leaving ~22ms of margin before the production retry loop's ~25ms first re-read) to remove a real flake risk under CI/scheduler/`-race` load |
| `main_test.go` | `parseLogLevel`'s recognized values and its unrecognized-value fallback-to-info behavior; the flag-parsing exit-code contract (`-h`/`-help` exits 0, an unrecognized/malformed flag exits 1, and now a stray non-flag positional argument -- most likely a mistyped flag missing its leading dash -- also exits 1 with a clear stderr message) via a re-exec'd subprocess; `detectSwallowedFlagValue` directly across guarded/unguarded flags, dash-only values, self-matches, and values that merely look flag-like without matching any registered flag; `main()`'s own `-collect`+`-interactive` call site (not just `runCrossServerTest`'s) proven to reach `RunInteractive` despite a benign `CollectAll` business-logic failure; `main()`'s `FetchBuildings` fallback failure with `-interactive` set proven to also still reach `RunInteractive`; the zero-buildings fallback proven to preserve an already-obtained non-empty `visitors` slice instead of letting the fallback's own (likely-empty) result clobber it; and `TestRunCrossServerTestPortExplicitButInvalidWording` now asserting the invalid-port message text plus a separate `port=-1` structured field, matching `-cs-port`'s move from an inline-formatted message to a structured slog field |
| `main_flags_test.go` | `decodeModeIgnoredFlags` and `ignoredCrossServerFlags`'s exempt-flag filtering (`decode-stream`/`decode-label`/`log-level`, and the redirect-gating `cs-ip`/`cs-rt`, are never reported as ignored); `refreshHasUsableData`'s At-token-or-nonempty-ServerList check gating whether a GSL `opt=refresh` response is usable; a regression test scanning main.go's own `fs.String/Bool/Int("cs-...", ...)` declarations to keep `crossServerFlagNames` in sync with the FlagSet as flags are added, renamed, or removed; `warnIfDecodeLabelIgnored`'s warning firing only when `-decode-label` is set without `-decode-stream`; `warnIfExplicitConfigPathNotFound` warning only for an explicit, nonexistent `-config` path (not a nonexistent default path, not when `-no-config` is also set); `serverListOverrideFlags` no longer reporting an explicitly-typed-but-empty/zero `-cs-ip`/`-cs-port`/`-cs-zone`/`-cs-gameuid` as an override; `shouldAbortBeforeInteractive`'s exit-vs-continue decision across nil/genuine-non-timeout-net.Error/benign-timeout-net.Error/plain-business-logic-error, each crossed with `-interactive` requested or not; and `stringFlagSwallowGuardNames` kept in sync with main.go's own `fs.String(...)` declarations (mirroring `crossServerFlagNames`' existing drift test) |
| `main_crossserver_test.go` | `crossServerSaveBackNeeded` (the pure comparison extracted from `runCrossServerTest`'s session-config save-back check), including the round-12 regression case where only the access token changed and the round-13 case where only `GameUid` changed (a `-cs-rt` refresh with no `serverInfo` redirect) -- both previously silently dropped since the save-back condition didn't compare them; an end-to-end `runCrossServerTest` test proving a `-cs-rt` refresh's fresh access token is actually persisted to the session config file even when no redirect occurs; `runCrossServerTest` exiting clearly (not silently dialing loopback) when the resolved ip is empty; a GSL `opt=refresh` response with no usable data exiting 2 (not 1); a `-cs-rt` refresh's server list overriding an explicitly-passed `-cs-ip`/etc. flag logging at WARN naming the overridden flag(s), versus a plain INFO when nothing explicit was overridden; a `-cs-rt` refresh leaving the run with zero access token at all (not merely an unrefreshed stale one) now warning distinctly, via a subprocess re-exec since `DoCrossServerLogin`'s own empty-token check would otherwise exit the in-process test; the `port <= 0` pre-flight guard now exiting clearly (mirroring its previously-untested sibling `firstHost(ip)==""` guard); a GSL refresh HTTP-level failure now exiting 1 via a fake GSL server returning an HTTP error; `-collect`+`-interactive` end to end proving benign-only `CollectAll` failures no longer block reaching interactive mode, via a fake game server that fails every collect action with a fast decoded errorCode (not a real 8s timeout); `runCrossServerTest`'s port-validation wording distinguishing an explicitly-passed-but-invalid port from one never given at all; `crossServerSaveBackNeeded` correctly returning false for a pipe-delimited `origHost` whose `firstHost` matches the resolved `newHost`; `runCrossServerTest`'s `FetchBuildings` fallback failure with `-interactive` set proven to still reach `RunInteractive`; `runCrossServerTest` exiting clearly with an empty `gameUid` even after a `-cs-rt` refresh that supplies a fresh access token but an empty server list; and an explicitly-passed-but-empty `-cs-ip`/`-cs-gameuid` now logging wording distinct from "never given at all" (mirroring the existing port-validation distinction) |
| `config_test.go` | Session config load/save: explicit-path loading, loose-file-permission warnings, permission tightening on save; `loadEffectiveConfig` returning `(nil, "")` silently on a genuinely absent default path, and exiting with a clear error (via a re-exec'd subprocess, asserting exit code and stderr) on a non-`ENOENT` read failure (a directory sitting where the file is expected) instead of silently treating it the same as absent; `SaveSessionConfig`'s normal write/read-back round trip still working correctly now that it goes through the atomic write-temp-then-rename helper |
| `login_test.go` | `gslOptFor`'s 3-way opt-priority order (`login` > `fix` > `new`, including the case where `LoginKey` and `GameUid` are both set); `redact`'s secret-masking for log output, including a CJK name-shaped input and an internationalized-email-shaped input, proving the redacted output is always valid UTF-8, a realistic 9-20 character password-length input proving no more than a small minority of it appears unredacted, and a long (40-char) opaque-token input proving the first-4/last-4 shape stays useful there |
| `login_integration_test.go` | `Login()` exercised end to end against a fake CheckVersion/GetServerList HTTP endpoint and `net.Listen`-based fake SFS2X game servers (reusing crossserver_test.go's fake-server helpers): the guest happy-path dial+login+init-push flow with no redirect; serverInfo-redirect handling refreshing `gameUid` mid-redirect onto both the returned `Ident` and the persisted identity file (the `Login()` counterpart to crossserver_test.go's `DoCrossServerLogin` redirect test); the `maxRedirectHops`-exceeded bound (also mirroring its `DoCrossServerLogin` counterpart); the email-verification path (send-code/wait-for-code/account.login.new/push.account.login.new) persisting a real, non-guest loginKey/gameUid/username; and `Login()`'s captured log output proven to never contain the raw verification code, deviceId, airKey, or operator email address (only their lengths); a fake server that closes the connection instead of ever sending `init` proven to make `Login()` return a clear error immediately (the round-17 `initErr != nil` fix), not a nominally-successful result wrapping a dead connection; a persist failure on the received `gameUid`/`gameUserName` at the end of the email-verification path proven to log the expected warning while `Login()` still returns success; `waitForInitPush`'s `init`-push return deduping a repeated building uuid and visitor uid within a single push down to one occurrence each; and an end-to-end proof that a present-but-wrong-typed building `uuid` in the primary `init`-push path is rejected by `ParseInitBuildings`' type-aware guard rather than silently coercing to `uuid=0` and being mistaken for a duplicate of a genuine `uuid=0` entry |
| `buildings_visitors_test.go` | `BuildingNameOf`, `collectCmdFor`, and init-push building/visitor parsing (`ParseInitBuildings`/`ParseInitVisitors`, including malformed-entry skipping); `ParseInitBuildings` capping the number of RAW `building_new` items examined at `maxRawBuildingItemsPerPush`, not just valid ones appended to the output; `FetchBuildings`' `push.add.building` AND `push.init.build` inline loops each applying the identical raw-item-scan cap, the latter proven end to end over a fake `net.Pipe` server; an exactly-at-cap/cap+1 boundary test proving the cap's `>` condition doesn't spuriously trigger on a legitimately at-cap-length response; and `ParseInitBuildings` rejecting a present-but-wrong-typed `uuid` (e.g. sent as a string) instead of silently coercing it to `uuid=0` |
| `pure_helpers_test.go` | Pure helpers spanning buildings/mail/alliance: `collectibleBuildings`, `groupUnclaimedByType`, `findRecommendedTech`; `Mail.HasUnclaimedReward`'s explicit-null-vs-missing `rewardStatus` guard and `groupUnclaimedByType`'s matching guard for a missing/null `type` field, both proving a genuinely-absent field (notification-only mail) is excluded rather than misclassified as a real zero value |
| `redirect_helpers_test.go` | GSL/cross-server helpers: `findServerInfo`, `getIntFlexible`, `serverIDFromZone` |
| `interactive_test.go` | `putJSONValue`'s JSON-to-`SFSObject` type mapping, including `json.Number`'s int64-vs-float64 fallback for uuid-sized values |
| `alliance_test.go` | `HelpAllianceMembers` and `ClaimAllianceGifts` (both gift types, Premium then Regular, in order, aborting the 2nd type immediately on a genuine, non-timeout `net.Error` while still attempting it on an ordinary decoded errorCode failure or a `net.Error` with `Timeout()==true`, now via the shared package-level `fakeNetErrConn`/`fakeNetError` fixture instead of a local duplicate); `findRecommendedTech`'s state==1/scienceId selection, including the regression where an explicit-null `scienceId` must be skipped rather than falling through to `scienceId=0`; `DonateRecommendedAllianceTech`'s three no-op branches plus its real donate call; the documented `al.science.donate` cooldown (`errorCode=120471`) benign-no-op classification; `DonateRecommendedAllianceTech` logging a warning (naming the actual Go type via `%T`, never `SFSValue.String()`'s hardcoded redaction placeholder) when `allianceScience` is present but not an array; `ClaimAllianceGifts` aborting on a real graceful close (bare `io.EOF` through an actual `GameConn`, not just a synthetic `net.Error` fake); `findRecommendedTech` capping the number of RAW `allianceScience` items examined at `allianceScienceRawItemCap`, not just valid ones matched, including an exactly-at-cap/cap+1 boundary test; and `findRecommendedTech` rejecting a present-but-wrong-typed `scienceId` instead of silently coercing it to `scienceId=0` |
| `crossserver_test.go` | `DoCrossServerLogin` against real `net.Listen`-based fake servers (it dials its own connection, so it can't use `net.Pipe` like the other orchestration tests): the no-redirect success path, following a single `serverInfo` redirect, the `maxRedirects` bounded-loop guard erroring out instead of looping forever, the mid-redirect GSL refresh propagating a fresh `gameUid` into both the top-level `un` field and the nested login-params `gameUid` field, the `LWDEBUG_DUMP_LOGIN` debug dump proven to never leak the raw access token/`shumeiBoxId` (including with `IOSMode: true`) or `LWDEBUG_DUMP_LOGIN_BODY`'s dump file always ending up `chmod`'d 0600 even when it pre-existed with looser permissions, the experimental `-handshake` path's "handshake OK" log proven to never leak the Handshake response's `tk` session token; `DoCrossServerLogin`'s own mid-login `serverInfo` redirect rejecting a malformed/empty redirect ip clearly instead of silently dialing loopback; and `DoCrossServerLogin`'s INITIAL dial now also rejecting an empty ip/non-positive port, not just its redirect branch |
| `mail_orchestration_test.go` | `ListMail`'s pagination loop (`lastUid`/`lastMailTime` carried into the next request, `firstCmd` sent only on the cold-start page), a `more=true` response with a missing `lastUid` correctly stopping pagination instead of looping on a stale cursor, a server that never stops reporting `more=true` correctly warning once the 20-page cap is reached instead of truncating silently, `ClaimAllMail` still processing already-collected mail after a mid-pagination `ListMail` failure instead of discarding it, `ClaimAllMail`'s batching under both the 100-item count cap and the 60000-byte length cap, proving batches split at the right boundary with no uid dropped, duplicated, or reordered, `ClaimAllMail` aborting all remaining read-status and reward-claim batches immediately on a genuine, non-timeout `net.Error` (and skipping the reward-claim loop entirely when the read-status loop itself aborted that way), reward-claim batches for multiple distinct mail types still all running when only an ordinary decoded errorCode failure (not a `net.Error`) occurs, `ListMail` itself failing with a non-timeout `net.Error` after already collecting partial mail causing `ClaimAllMail` to return promptly without issuing any read-status batch call at all; a non-timeout `net.Error` firing on the FIRST of >=2 distinct mail types' reward-claim batches proven to abort the SECOND type's request entirely, not just the rest of the first type's batches; `ListMail` returning the same uid across two consecutive pages proven to appear only once in the returned slice; a `net.Error` with `Timeout()==true` at each of the three abort sites (post-`ListMail`, the read-status loop, the reward-claim loop) proven to NOT abort -- the run continues and the timeout is still recorded in the aggregated error; `ListMail` warning when the response's `more` field is present but not a bool; `ListMail` warning when `lastMailTime` is missing/null on a `more=true` page; `ClaimAllMail` aborting on a real graceful close (bare `io.EOF` through an actual `GameConn`, not just a synthetic `net.Error` fake); `ListMail` capping the number of RAW per-page mail items examined at `mailListRawItemCap`, not just valid ones appended to the output, including an exactly-at-cap/cap+1 boundary test; `ListMail` rejecting a present-but-wrong-typed `uid` instead of silently coercing it to `uid=""` and permanently losing that mail's dedup/claim tracking; and `groupUnclaimedByType` rejecting a present-but-wrong-typed `type` field instead of silently merging it into a genuine `type=0` batch |
| `buildings_orchestration_test.go` | `FetchBuildings` parsing an `init` push's `building_new`/`visitor` fields and its timeout behavior with no data and with partial data; building uuids deduped across the `init`/`push.init.build`/`push.add.building` sources so a redundant collect call is never issued twice for the same building; visitor uids likewise deduped across repeated `init` pushes; its unrecognized-push log lines proven to never leak a raw credential; `CollectIdleReward`'s peek-then-claim two-call sequence; `CollectAll` aggregating a genuine mid-sequence failure (`al.help.all`) without short-circuiting the remaining calls, aborting its remaining sub-actions early on a genuine, non-timeout `net.Error` (proven via a fake connection whose every `Read` fails with one) instead of still waiting out every remaining timeout, and a `net.Error` with `Timeout()==true` proven to NOT abort -- every one of `CollectAll`'s fixed sub-actions still gets attempted; `FetchBuildings`' per-push deadline extension staying capped at the caller's original timeout even against 5 qualifying `init` pushes sent closer together than the 3-second extension window (a connection kept open throughout, so the test can only pass via the deadline cap actually firing, not via an early `io.EOF`); `containsNonTimeoutNetError` proven correct directly (nil, bare timeout, bare non-timeout, and both orderings of a joined timeout+non-timeout pair); `CollectAll` proven to still abort when a sub-action's own internally-joined error buries a genuine non-timeout `net.Error` behind an earlier benign timeout (the exact masking `errors.As` alone would miss); `containsNonTimeoutNetError` also proven to unwrap through an intermediate `fmt.Errorf`-wrapped node (`ClaimAllMail`'s own error-wrapping shape for a `ListMail` failure) before finding the underlying `net.Error`, not just a bare one placed directly in a `Join`; `FetchBuildings` proven to still wait for and capture the authoritative `init` push's data when a `push.init.build` arrives first with a caller timeout that would have been prematurely exhausted by the pre-fix unconditional deadline-shrink; `CollectAll` truncating to `maxCollectibleBuildingsPerRun` with a warning when the collectible-building count exceeds the sanity cap; a new shared `realEOFConn` fixture (reused by `alliance_test.go`/`interactive_orchestration_test.go`) driving a real bare `io.EOF` through an actual `GameConn`, not a synthetic `net.Error` fake; and `CollectAll` aborting on that real graceful close end to end |
| `visitors_orchestration_test.go` | `GreetVisitors`: the empty-visitor-list short-circuit (nothing sent), one `visitor.operate` request per visitor in order, error aggregation that folds away the benign `visitor_err_coming` errorCode while still surfacing a genuine failure, without stopping at the first error, aborting all remaining visitors immediately on a genuine, non-timeout `net.Error`, a `net.Error` with `Timeout()==true` proven to NOT abort the remaining visitors; `ParseInitVisitors` truncating `visitor.list` to the `init` push's own `maxNum` (or `maxVisitorsDefensiveCeiling` if absent/nonsensical), with a warning when the server-sent list exceeds the cap; the fallback-ceiling path itself proven to actually truncate (not just the `maxNum`-driven path); an inflated server-supplied `maxNum` proven to clamp to `maxVisitorsUpperBound`, not the attacker-supplied value; `GreetVisitors` aborting on a real graceful close (bare `io.EOF` through an actual `GameConn`, not just a synthetic `net.Error` fake); `ParseInitVisitors` capping the number of raw items scanned/logged (not just valid output entries) when the list is padded with far more malformed entries than the configured cap, including an exactly-at-cap/cap+1 boundary test for the fallback ceiling; and `ParseInitVisitors` rejecting a present-but-wrong-typed `uid` instead of silently coercing it to `uid=0` |
| `interactive_orchestration_test.go` | `handleInteractiveLine` over a `net.Pipe`-backed connection: aborts without sending anything when a parsed JSON value has no `putJSONValue` case, correctly parses/sends a well-formed `cmd.name {json}` line as an Extension call, its "sending command"/"received response" log lines are proven to never leak a raw `loginKey`/`pw` from either the outgoing params or the incoming response, a malformed-JSON command line is proven to never leak its raw unparsed params text (which could itself contain a credential the operator was about to send) into the "bad JSON params" error log, trailing garbage after a well-formed JSON value aborts the send instead of being silently discarded; a missing space between the command name and its JSON params is proven to abort with a clear error (never leaking the glued-on raw line) instead of silently sending the mangled whole line as a literal command; a genuinely bare command with no params and no braces is proven to still send normally, unaffected by that check; a `Timeout()==true` net.Error from the response wait is proven to stay non-fatal; a non-timeout net.Error from the response wait is proven to exit the process (via a re-exec'd subprocess), matching the adjacent send-failure's fatal treatment; and `handleInteractiveLine` aborting on a real graceful close (bare `io.EOF` through an actual `GameConn`, not just a synthetic `net.Error` fake) |
| `decode_test.go` | `DecodeStreamFile`'s three branches against real `EncodePacket(EncodeObject(...))` output written to a temp file: clean end-of-stream after well-formed packets, a truncated-frame error naming the byte offset, and a corrupt-`SFSObject`-body packet whose `DecodeObject` error is logged inline while the stream continues decoding the packets that follow rather than aborting; a `push.account.login.new`-shaped packet proven to never print its raw `loginKey` to the decoded-stream output; and a truncated packet with an intact sensitive field (e.g. `tk`) ahead of the truncation point proven to never leak that field's value (raw or hex-encoded) via the `DecodeObject`-failure diagnostic line |
| `credential_leak_lint_test.go` | A repo-wide, recursive-directory regex scan of every non-test `.go` file for a `slog.*`/`fmt.*`/`log.*` sink call that embeds a raw `.String()`/`.unsafeRawString()` call (joining multi-line calls first, threading raw-string state across lines and stripping string/rune-literal contents and `//` comments before counting parens or matching so neither a stray `)` nor a multi-line backtick string can defeat the join) -- the credential-leak pattern this repo has hit repeatedly -- failing the build on any new, un-allowlisted instance instead of waiting for the next audit round to find it. Since round 14, `SFSObject.String()` itself is safe by default (see sfsobject.go), so this guard's live purpose is now catching a call to the `unsafeRawString()` escape hatch from outside sfsobject.go, with plain `.String()` still flagged for defense-in-depth; round 28 added a sibling scan (`TestNoSensitiveGetStringLoggedRaw`) catching a *single field's* raw value reaching a log/error sink via `.GetString(sensitiveKey)` embedded directly in the sink call -- the exact shape of round 28's `login.go` `un`-field cleartext leak, which bypassed `StringRedacted()` entirely since it never dumps a whole object |
| `vip_test.go` | `ClaimVIPDailyLoginScore` and `ClaimVIPDailyFreebie`: exact cmd string sent with empty params on the success path, and the documented benign already-claimed-today cooldown (`errorCode=120289`) returning nil, not an error |
| `sfsobject_fuzz_test.go` | Native Go fuzz targets `FuzzDecodeObject`/`FuzzReadPacket`, seeded with real encoded fixtures plus this repo's existing hostile-input edge cases (nesting/node-count bombs, negative counts, oversized frames), asserting only that the wire-format parsers never panic on malformed input; run for a bounded time in CI as defense-in-depth alongside the hand-written edge-case tests |
| `tools/reassemble_stream.py` | Reassembles one TCP stream from a pcap into `-decode-stream`-ready files — see [Capturing and decoding traffic](docs/capturing-and-decoding-traffic.mdx) |

## License

Apache License 2.0 -- see [LICENSE](LICENSE).
